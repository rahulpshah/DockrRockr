{
  "name": "deploy",
  "version": "0.0.0",
  "description": "\"Deploy on AWS\"",
  "main": "aws.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "needle": "^0.7.10",
    "aws-sdk": ">= 2.0.9"
  },
  "dependencies": {
    "dateformat": "^2.0.0"
  }
}
